name: Gemini CLI Run
on: [push]
jobs:
  run-gemini:
    runs-on: ubuntu-latest
    steps:
      - name: Gemini parancssori parancs futtatása
        uses: google-github-actions/run-gemini-cli@v0.1.14
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
